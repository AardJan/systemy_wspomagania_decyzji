%% atoms
coverage.
doors.
gates.
windows.
cockpit.
wings.
lamps.
insciption.
image.

fly.
take_off.
landing.
crush.

%% facts
have(coverage).
have(doors).
have(gates).
have(windows).
have(lamps).
have(wings).
have(cockpit).
have(insciption).
have(image).

can(fly).
can(take_off).
can(landing).
can(crush).